# Google Sheets: create a Service Account at https://console.cloud.google.com/
# Download JSON key and save as credentials.json in project root (or set path below).
# Share your Google Sheets with the service account email.

GOOGLE_CREDENTIALS_JSON=credentials.json

# Optional: Sheet IDs (from the URL: .../d/SHEET_ID/...)
# Create 3 sheets: Pilot Roster, Drone Fleet, Missions; paste IDs here.
PILOT_SHEET_ID=
DRONE_SHEET_ID=
MISSIONS_SHEET_ID=

# Optional: OpenAI for richer conversational responses (leave empty for intent-based agent)
OPENAI_API_KEY=
